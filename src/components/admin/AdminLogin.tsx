"use client";
import { FormEvent, useState } from "react";
export function AdminLogin() { const [status, setStatus] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ secret: form.get("secret") }) }); if (!response.ok) { setStatus("Clave incorrecta o acceso bloqueado temporalmente."); return; } window.location.reload(); }
return <div className="mx-auto max-w-md px-4 py-20"><h1 className="text-3xl font-semibold">Admin MVP</h1><p className="mt-2 text-sm text-slate-600">Acceso interno. La clave vive en variables de entorno.</p><form onSubmit={submit} className="mt-6"><label className="text-sm font-medium">Clave administrativa<input name="secret" type="password" required className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" /></label><button className="mt-4 w-full rounded-xl bg-slate-950 px-4 py-3 font-medium text-white">Entrar</button><p aria-live="polite" className="mt-3 text-sm text-red-700">{status}</p></form></div>; }
